"""pipeline_b generation engine.

M1: structural (tectonic) stage only — see MILESTONES.md. Every module
keeps the core algorithmically portable (plain array math, clean stage
boundaries) for the eventual native-language port.
"""

VERSION = "0.3.1-m3"
